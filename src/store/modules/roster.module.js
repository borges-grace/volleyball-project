

const state = {
    roster: { isLoading: false, data: new Map(), errMsg: null}
};

const actions = {};

const mutations = {};

// const helpers = {};

const getters = {};

export default {
    name: "roster",
    namespaced: true,
    state,
    getters,
    actions,
    mutations
}